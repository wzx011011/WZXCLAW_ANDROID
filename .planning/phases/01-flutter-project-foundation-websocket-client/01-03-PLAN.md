---
phase: 01-flutter-project-foundation-websocket-client
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - android/app/src/main/res/values/styles.xml
  - android/app/src/main/res/drawable/launch_background.xml
  - android/app/src/main/AndroidManifest.xml
autonomous: true
gap_closure: true
gap_closure_reason: "Fixes VERIFICATION.md Gap 1: missing Android resource files (styles.xml, launch_background.xml, res/ directory) and INTERNET permission. App cannot build without these."

requirements:
  - CONN-01
  - CONN-02

must_haves:
  truths:
    - "Android Gradle build can resolve @style/LaunchTheme and @style/NormalTheme from styles.xml"
    - "App launches with a valid launch background drawable"
    - "WebSocket connections are not blocked by Android OS (INTERNET permission granted)"
  artifacts:
    - path: "android/app/src/main/res/values/styles.xml"
      provides: "LaunchTheme and NormalTheme style definitions referenced by AndroidManifest.xml"
      contains: "LaunchTheme"
    - path: "android/app/src/main/res/drawable/launch_background.xml"
      provides: "Launch screen background drawable"
      contains: "shape"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android manifest with INTERNET permission"
      contains: "android.permission.INTERNET"
  key_links:
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android/app/src/main/res/values/styles.xml"
      via: "@style/LaunchTheme and @style/NormalTheme references"
      pattern: "@style/LaunchTheme"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android.permission.INTERNET"
      via: "uses-permission declaration"
      pattern: "android.permission.INTERNET"
---

<objective>
Fix the Android build blockers identified in VERIFICATION.md Gap 1: create the missing Android resource directory structure, add styles.xml with LaunchTheme and NormalTheme, create the launch background drawable, and add INTERNET permission to AndroidManifest.xml.

Purpose: Without these files, the Gradle build fails with resource resolution errors before any Dart code executes. This is a blocking gap -- the app cannot compile or run.

Output: A Flutter Android project with complete Android resource files that builds successfully with Gradle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flutter-project-foundation-websocket-client/01-VERIFICATION.md

## Current AndroidManifest.xml (lines 1-26)

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="wzxClaw Android"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            ...>
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
        </activity>
    </application>
</manifest>
```

References `@style/LaunchTheme`, `@style/NormalTheme`, and `@mipmap/ic_launcher` -- none of these resources exist yet.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Android resource directory structure with styles.xml and launch drawable, add INTERNET permission</name>
  <files>android/app/src/main/res/values/styles.xml, android/app/src/main/res/drawable/launch_background.xml, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    E:\ai\wzxClaw_android\android\app\src\main\AndroidManifest.xml
  </read_first>
  <action>
1. Create the directory structure:
   - `android/app/src/main/res/values/`
   - `android/app/src/main/res/drawable/`

2. Create `android/app/src/main/res/values/styles.xml` with the following exact content:
   ```xml
   <?xml version="1.0" encoding="utf-8"?>
   <resources>
       <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
           <item name="android:windowBackground">@drawable/launch_background</item>
       </style>
       <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
           <item name="android:windowBackground">@android:color/black</item>
       </style>
   </resources>
   ```
   This defines LaunchTheme (used by the activity at launch, shows the launch_background drawable) and NormalTheme (used after Flutter initializes, standard dark background). Both inherit from Theme.Black.NoTitleBar to match the app's dark theme.

3. Create `android/app/src/main/res/drawable/launch_background.xml` with the following exact content:
   ```xml
   <?xml version="1.0" encoding="utf-8"?>
   <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
       <item>
           <shape android:shape="rectangle">
               <solid android:color="#1A1A2E"/>
           </shape>
       </item>
   </layer-list>
   ```
   This creates a solid dark background (#1A1A2E) matching the app's dark theme scaffold background color. The splash screen will show this color before Flutter renders.

4. Update `android/app/src/main/AndroidManifest.xml` to add the INTERNET permission. Insert `<uses-permission android:name="android.permission.INTERNET" />` as a direct child of the `<manifest>` element, BEFORE the `<application>` tag. The final manifest must look like:
   ```xml
   <manifest xmlns:android="http://schemas.android.com/apk/res/android">
       <uses-permission android:name="android.permission.INTERNET" />
       <application
           android:label="wzxClaw Android"
           android:icon="@mipmap/ic_launcher">
           <activity
               android:name=".MainActivity"
               android:exported="true"
               android:launchMode="singleTop"
               android:theme="@style/LaunchTheme"
               android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
               android:hardwareAccelerated="true"
               android:windowSoftInputMode="adjustResize">
               <meta-data
                 android:name="io.flutter.embedding.android.NormalTheme"
                 android:resource="@style/NormalTheme"
                 />
               <intent-filter>
                   <action android:name="android.intent.action.MAIN"/>
                   <category android:name="android.intent.category.LAUNCHER"/>
               </intent-filter>
           </activity>
           <meta-data
               android:name="flutterEmbedding"
               android:value="2" />
       </application>
   </manifest>
   ```
   The INTERNET permission is required for WebSocket connections. Without it, Android blocks all network access at the OS level.
  </action>
  <verify>
    <automated>cd E:/ai/wzxClaw_android && test -f android/app/src/main/res/values/styles.xml && test -f android/app/src/main/res/drawable/launch_background.xml && grep -q "android.permission.INTERNET" android/app/src/main/AndroidManifest.xml && echo "ALL FILES AND PERMISSION VERIFIED" || echo "MISSING FILES OR PERMISSION"</automated>
  </verify>
  <done>
    - android/app/src/main/res/values/styles.xml exists with LaunchTheme and NormalTheme style definitions
    - LaunchTheme parent is @android:style/Theme.Black.NoTitleBar with windowBackground pointing to launch_background drawable
    - NormalTheme parent is @android:style/Theme.Black.NoTitleBar with android:color/black windowBackground
    - android/app/src/main/res/drawable/launch_background.xml exists with a solid #1A1A2E background matching app dark theme
    - AndroidManifest.xml contains `<uses-permission android:name="android.permission.INTERNET" />` as direct child of `<manifest>`
    - Android Gradle build will no longer fail on resource resolution for @style/LaunchTheme or @style/NormalTheme
    - WebSocket connections will not be blocked by Android OS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AndroidManifest -> OS | Declares required permissions to Android framework |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | E (Elevation of Privilege) | INTERNET permission | accept | Required for WebSocket connectivity. Standard Android permission. No sensitive data exposure -- app only connects to user's own wzxClaw desktop. |

</threat_model>

<verification>
1. `android/app/src/main/res/values/styles.xml` exists and contains "LaunchTheme" and "NormalTheme" strings
2. `android/app/src/main/res/drawable/launch_background.xml` exists and contains a valid shape drawable
3. `android/app/src/main/AndroidManifest.xml` contains `android.permission.INTERNET` permission
4. All XML files are well-formed (valid XML syntax)
5. LaunchTheme references @drawable/launch_background which now exists
6. NormalTheme references @android:color/black which is a system resource
7. styles.xml parent theme (@android:style/Theme.Black.NoTitleBar) is a standard Android theme
</verification>

<success_criteria>
- Android res/values/styles.xml defines LaunchTheme and NormalTheme
- Android res/drawable/launch_background.xml provides a dark splash background
- AndroidManifest.xml declares INTERNET permission
- No resource resolution errors remain -- the Gradle build can proceed past the resource linking phase
- The VERIFICATION.md Gap 1 (build blockers) is fully resolved
</success_criteria>

<output>
After completion, create `.planning/phases/01-flutter-project-foundation-websocket-client/01-03-SUMMARY.md`
</output>
